#include <stdio.h>

enum
{
    branco,
    cinza,
    preto,
};

int dfs(int **matrix, int *color, int *time, int size)
{
    for (int i = 0; i < size; i++)
    {
        color[i] = branco;
    }
};

int dfs(int **m, int size)
{

    int cor[size];
}
